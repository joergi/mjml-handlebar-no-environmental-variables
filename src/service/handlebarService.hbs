
export const initializeHandlebars = (app: Express) => {
    app.set("view engine", "hbs");

    app.engine("hbs", hbs({
        defaultLayout: "default",
        extname: ".hbs",
        helpers: helpers()
    }));


};
